use super::Input;
use super::part1::solve as part1_solve;

pub fn solve(input: &Input) -> usize {
    // exactly the same as part 1, because the input is parsed differently within `mod.rs`
    part1_solve(input)
}
